import { describe, it } from "vitest";

describe("parser", () => {
	it.todo("parses a minimal .product.mdx file");
	it.todo("parses a full .product.mdx file with nested sections");
	it.todo("throws on invalid MDX syntax");
	it.todo("extracts block attributes correctly");
});
